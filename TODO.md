# 项目待办事项

## 角色清单遗漏

- [x] **朱亮** ✅ 已补充到 step1 角色清单，角色圣经已创建

## 待定决策

- [x] **角色圣经 B 板块（阶段场景提示词）去留问题** ✅ 已决定：除非有必要，否则只保留一个默认阶段，不分多个阶段

- [ ] **Character Sheet 完整度提升**（待根据生成质量决定是否开发）
  - 现状：角色圣经第九章目前只有多角度转面图（Turnaround）
  - 缺失板块：
    1. **Expression Sheet（表情表）**——同一角色6-8种关键表情，确保不同表情下角色可辨认
    2. **Costume Variations（服装/阶段变体）**——不同故事阶段的着装变化，每套服装多角度展示
    3. **Scale Reference（比例参考）**——多角色并排站立，展示身高和体型相对关系
    4. **Detail Callouts（细节特写）**——标志性道具、饰品、伤疤等关键细节放大图，附材质和颜色标注
  - 决策时机：Step 4 视觉资产生成后，根据实际生成质量评估是否需要补充这些板块

## Character Skill - director_preference 机制

- [ ] **新增脚本：init_director_preference**
  - 遍历每个角色文件夹，生成 `director_preference` 文件
  - 初始内容固定为：`暂无，请从原始小说中提取。`

- [ ] **调整 character skill 流程**
  - 先运行上述脚本，确保每个角色有 director_preference 文件
  - 生成角色圣经时的优先级：
    1. director_preference 有导演填写的内容 → **以导演填写为准**
    2. director_preference 为默认值（暂无）→ **从小说原始文件中提取**
    3. 小说中也没有 / 导演想自己指定 → **用预填写模板**

- [ ] **可选：预填写脚本**
  - 当小说缺失或导演想自定义时，用脚本预填写结构化模板：
    ```
    发型：
    脸型：
    衣着：
    ```
  - 导演填写后，角色圣经以此为准

## 待修复

- [x] **人物外貌区分度保障** ✅ 已通过 Director Vision 解决，导演已为核心角色逐一设定选角偏好

- [ ] **场景图片风格统一：以标杆场景为参考**
  - 后续场景生成的图片风格应对齐以下已有的优秀场景：永安镇、上神界、凤廊山脉、半山腰秘密仓库、县城破庙 默认002、天德城 繁华期00、望海河
  - 需要提取这些场景的共性风格特征，作为全局 art direction 的基准

- [x] **场景 prompt 严禁出现人物** ✅ 已在 SKILL.md 中强化规范，所有场景圣经已重新生成并加入 negative prompt

- [ ] **镜头切换方式优化**
  - 现状：`production/step5-storyboard/shot_list_merged.md` 中绝大多数镜头都是硬切，缺乏转场变化
  - 需要：根据叙事节奏和情绪添加合适的转场方式（淡入淡出、叠化、推拉等）

## 待解决的技术问题

- [ ] **多角色同框场景的生图方案** 🔴 高优
  - 问题：影视中大量场景涉及多个重要角色同框（群戏、会议、对峙等），当前 pipeline 的角色参考图体系是"单人参考"模式，无法处理：
    - 身份不串：多角色同时出现时，生图模型容易将 A 的服装/脸型混到 B 身上
    - 空间站位：谁在左、谁在中、谁背对镜头——纯文字 prompt 难以精确控制
    - 交互关系：人物之间的对话朝向、视线交汇、肢体互动，文字描述精度不够
    - 参考图分配：如何让模型知道"这张参考图对应画面中左边那个人，那张对应右边那个人"
  - 可能的技术方向（待调研）：
    - 分层合成：先单独生成每个人，再合成到同一场景
    - 布局控制：用 bounding box / pose skeleton 指定每个人的位置和姿态
    - Inpainting 逐人绘制：先生成场景，再逐区域填入不同角色
    - ControlNet + 多参考图：通过 pose/depth 条件控制 + 分区参考图
    - 商业方案调研：Seedream / Kling / 可灵等是否已支持多角色一致性
  - **即梦/Seedance 2.0 的 @引用方案**（已初步调研）：
    - Seedance 2.0（2026.2 发布）支持"全方位参考"模式，最多上传 **12 个参考文件**（9图+3视频+3音频）
    - 上传后系统自动分配标签 `@Image1`、`@Image2`…，在 prompt 中用 `@` 引用
    - **多角色 prompt 写法**：`@Image1 作为角色A的面部特征参考，@Image2 作为角色B的面部特征参考，角色A（描述）站在画面左侧，角色B（描述）站在画面右侧`
    - 参考维度可设为"角色特征"，参考强度建议 70%（>80%姿势僵硬，<50%五官变形）
    - 导入多个人物参考图时，参考维度自动切换为"人物合影"模式
    - **核心价值**：这是目前唯一支持在 prompt 中显式区分多个角色参考图的商业方案（Sora/Kling/Veo 均无同等能力）
    - **待验证**：实际多人同框效果（身份串扰程度）、空间站位控制精度、与我们 pipeline 的集成方式
  - 影响范围：step5（分镜脚本）、step6（首帧生成）、step7（视频生成）

## 问题

* 现在让我们回到场景圣经中来看一个问题：
泊岗镇是一个特别大的场景，它里面包含了很多小场景，这些小场景才是真正需要的，然而都被缺失了。是因为清单中没有提取码？

* 餐馆生成出来的太脏乱差，整个art direction 需要调整或者没有应用到 prompt。或者，整个 skill 需要暴露出一个可以允许差异化调整的机制口子
